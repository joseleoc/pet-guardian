import { SafeAreaView, Text } from "@/src/components/atoms";

export default function AuthenticateScreen() {
  return (
    <SafeAreaView>
      <Text type="title">Authenticate</Text>
    </SafeAreaView>
  );
}
